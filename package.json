{
  "name": "auto-x-smart-garage",
  "private": true,
  "workspaces": [
    "front-end",
    "back-end"
  ],
  "dependencies": {
    "express": "^4.22.2"
  },
  "overrides": {
    "mime-types": "2.1.35"
  },
  "scripts": {
    "build": "npm --prefix front-end run build",
    "start": "npm --prefix back-end start",
    "start:frontend": "npm --prefix front-end start",
    "start:backend": "npm --prefix back-end start"
  }
}
